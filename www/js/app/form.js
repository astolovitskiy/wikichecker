// Generated by CoffeeScript 1.7.1
var processErrors;

processErrors = function() {
  var input, key, val, _ref;
  if (window.errors !== void 0) {
    _ref = window.errors;
    for (key in _ref) {
      val = _ref[key];
      input = $('#' + formName + '_' + key);
      input.attr('data-content', val);
      input.popover();
      input.click();
      input.on('focus', function() {
        var $el = $(this);
        $el.popover('disable');
        return $el.parent().find('.popover').remove();
      });
    }
    return true;
  }
};

$(document).ready(function() {
  return processErrors();
});
